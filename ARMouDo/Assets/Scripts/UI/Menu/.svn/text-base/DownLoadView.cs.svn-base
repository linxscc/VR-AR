/*
 *    日期:2017,8,18
 *    作者:
 *    标题:
 *    功能:下载管理
*/
using UnityEngine;
using System.Collections;
namespace PlaceAR
{
    /// <summary>
    /// 下载管理
    /// </summary>
    public class DownLoadView : MonoBehaviour
    {
        public GameObject delAlert;
        //[SerializeField]
        public GameObject downlodingGrid;
       // [SerializeField]
        public GameObject downloadedGrid;
        public GameObject select;
        public GameObject selectAll;
        //  [SerializeField]
        //  private GameObject goBack;
        // Use this for initialization
        /// <summary>
        
        /// <summary>
        /// 返回
        /// </summary>
        public void Back()
        {
            
            DownLoadViewControl.Singleton.Close();
        }
        
        void Start()
        {
            OnInit();
        }
        public void OnInit()
        {
            //downlodingGrid.SetActive(false);
           // downloadedGrid.SetActive(false);
        }
        // Update is called once per frame
        void Update()
        {

        }
    }
}