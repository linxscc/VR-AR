using UnityEngine;


public class Interlacte : MonoBehaviour
{
    private StereoCam stereoCam;
    bool isGet = false;
    string txt = "切割屏幕";
    private float hSliderValue = 0;
    // Use this for initialization
    void Start()
    {
        stereoCam = GetComponent<StereoCam>();
    }
    private void OnGUI()
    {
        //if (GUI.Button(new Rect(0, 0, 100, 50), txt))
        //{
        //    isGet = !isGet;
        //    if (isGet)
        //    {
        //        txt = "还原";
        //        stereoCam.stereo = StereoModes.Interlaced;
        //    }
        //    else
        //    {
        //        txt = "切割屏幕";
        //        stereoCam.stereo = StereoModes.Disabled;
        //    }
        //}
        //if (isGet)
        //{


        //    hSliderValue = GUI.HorizontalSlider(new Rect(0, 60, 100, 30), hSliderValue, 0.5F, 2F);
        //    stereoCam.parallaxDistance = hSliderValue;
        //}
        //print(hSliderValue);

    }
    // Update is called once per frame
    void Update()
    {

    }
}
